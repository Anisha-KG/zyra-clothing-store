<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Zyra Forgot Password</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

  <style>
    body {
      background-color: #e6d8d2;
      font-family: 'Segoe UI', sans-serif;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0;
    }

    .forgot-card {
      background: #fff;
      border-radius: 12px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      overflow: hidden;
      max-width: 900px;
      width: 100%;
      display: flex;
      flex-wrap: wrap;
    }

    .forgot-left {
      flex: 1 1 400px;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 40px;
    }

    .form-wrapper {
      max-width: 300px;
      width: 100%;
    }

    .forgot-left h2 {
      font-weight: 600;
      margin-bottom: 10px;
      color: #822a22;
      text-align: center;
    }

    .forgot-left p {
      color: #666;
      font-size: 14px;
      text-align: center;
      margin-bottom: 25px;
    }

    .forgot-left .form-control {
      border-radius: 6px;
      margin-bottom: 15px;
      padding: 10px 12px;
    }

    .forgot-left .otp-btn {
      background-color: #b75b44;
      border: none;
      width: 100%;
      padding: 10px;
      border-radius: 6px;
      color: white;
      font-weight: 500;
      margin-top: 10px;
      transition: 0.3s;
    }

    .forgot-left .otp-btn:hover {
      background-color: #9f4c39;
    }

    .forgot-left .back-link {
      display: block;
      margin-top: 15px;
      font-size: 14px;
      text-align: center;
      color: #822a22;
      text-decoration: none;
    }

    .forgot-left .back-link:hover {
      text-decoration: underline;
    }

    .forgot-right {
      flex: 1 1 400px;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }

    .forgot-right img {
      max-width: 90%;
      height: auto;
      border-radius: 8px;
      object-fit: cover;
    }

    @media (max-width: 768px) {
      .forgot-card {
        flex-direction: column;
      }

      .forgot-left,
      .forgot-right {
        flex: 1 1 100%;
        padding: 25px;
      }

      .form-wrapper {
        max-width: 100%;
      }

      .forgot-right img {
        max-width: 100%;
        border-radius: 8px;
      }
    }
  </style>
</head>

<body>
  <div class="forgot-card">
    <!-- Left Section -->
    <div class="forgot-left">
      <div class="form-wrapper">
        <h2>Forgot Password ?</h2>
        <p>Please confirm your mail address</p>

        <form action="/forgotPassword" method="POST">
          <input type="email" name="email" class="form-control" placeholder="Email address"  />
           <%if(typeof message!=='undefined'&&message){%>
            <div class="alert alert-danger text-center">
                <%=message%>
            </div>
            
            <%}
          %>
          <button type="submit" class="otp-btn">Send OTP</button>
        </form>

        <a href="/login" class="back-link">Go back to Login page</a>
      </div>
    </div>

    <!-- Right Section -->
    <div class="forgot-right">
      <img src="/images/forgotPassword.jpg" alt="Forgot Password Image" />
    </div>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
